# 0x02. Dart Classes

```
                __         __         __
            <(o )___   <(o )___   <(o )___
             ( ._> /    ( ._> /    ( ._> /
              `---'      `---'      `---'
        ~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~~
```

Object-oriented programming in Dart: classes, constructors, JSON
serialization (`toJson` / `fromJson`), private properties via the
underscore prefix, and inheritance with `extends`.

## Requirements

- Ubuntu 20.04 LTS
- Dart 3.x.x
- All files end with a new line

## Files

| File | Description |
|------|-------------|
| `0-class.dart` | `User` class with a `name` property and a `showName()` method |
